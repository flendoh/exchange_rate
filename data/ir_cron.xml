<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Cron job para actualizar las tasas de cambio diariamente a la 1:00 AM hora de Perú (6:00 AM UTC) -->
        <record id="exchange_rate_api_cron" model="ir.cron">
            <field name="name">Añadir Tasas de Cambio</field>
            <field name="model_id" ref="model_exchange_rate_api"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="state">code</field>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=6, minute=0, second=0, microsecond=0)"/>
            <field name="code">model.cron_call_api()</field>
        </record>
    </data>
</odoo>